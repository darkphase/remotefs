#!/bin/sh /etc/rc.common

START=50

LISTEN="0.0.0.0"
PORT="5001"
RFSD_USER="root"
PIDFILE="/var/run/rfsd.pid"

start()
{
	rfsd -a ${LISTEN} -p ${PORT} -u ${RFSD_USER} -r ${PIDFILE}
}

stop()
{
	killall rfsd
}

reload()
{
	kill -s HUP `cat ${PIDFILE}`
	start
}
